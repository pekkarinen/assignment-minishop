type Product {
  name: String!
  ean: ID!
  price: Float!
  imageUrl: String!
}

type OrderedProduct {
  product: Product!
  amount: Int!
}

type Order {
  orderId: ID!
  timestamp: String!
  customerId: String!
  products: [OrderedProduct!]!
  totalSum: Float!
}

type Query {
  products: [Product!]!
  product(ean: ID!): Product
  orders(customerId: ID!): [Order!]!
  order(orderId: ID!, customerId: ID!): Order
}

type Mutation {
  createOrder(customerId: String!): CreateModifyOrderResponse
  addProductToOrder(ean: String!, amount: Int!): CreateModifyOrderResponse
}

type CreateModifyOrderResponse {
  success: Boolean!
  message: String!
  order: Order! 
}
